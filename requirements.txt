# Pinned to the versions used to produce the released results.
# GPU: install the CUDA torch build matching your driver instead, e.g.
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cu126
# Results are reproducible per device (CPU vs CUDA RNG streams differ).
torch==2.12.0
numpy==2.4.6
matplotlib==3.10.9
PyYAML==6.0.3
pytest==9.0.3
